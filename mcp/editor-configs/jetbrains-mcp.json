{
  "_comment": "JetBrains MCP Configuration for ZombieCoder",
  "_usage": "Import this into Settings | Tools | AI Assistant | Model Context Protocol (MCP)",

  "mcpServers": {
    "zombiecoder-stdio": {
      "command": "node",
      "args": ["dist/mcp/server-entry.js"],
      "cwd": "${workspaceFolder}",
      "env": {
        "WORKSPACE_DIR": "${workspaceFolder}"
      },
      "enabled": true,
      "_description": "Recommended: local MCP server started by JetBrains AI Assistant"
    },
    "zombiecoder-bridge": {
      "url": "http://localhost:9999/mcp",
      "enabled": false,
      "_description": "Optional bridge: use if you want the running Proxi server instead of spawning local MCP"
    }
  }
}
